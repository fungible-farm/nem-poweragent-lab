# sim-gridy (placeholder)

Not part of the `labs/` AEMO training curriculum this repo otherwise contains. Kept as its own
separate module deliberately, on purpose, per the concept note at
[`docs/issues/sim-gridy-civilization-game.md`](../docs/issues/sim-gridy-civilization-game.md) — read
that file first, this one is just the placeholder marker.

Nothing is built here yet. No build tooling in this repo (`Justfile`, `pyproject.toml`, CI) knows
about this directory, and none of it should be wired in until there's a real plan — this is space
reserved, not a scaffold.

Not synced to GitHub or any web service yet.
